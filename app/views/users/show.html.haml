- provide :title, @user.name

= render partial: 'shared/events_feed', locals: {events: @hosted, name: 'hosted'}
= render partial: 'shared/events_feed', locals: {events: @upcoming, name: 'upcoming'}
= render partial: 'shared/events_feed', locals: {events: @past, name: 'past'}
